[project]
name = "librarysite"
version = "0.1.0"
dependencies = []

[tool.vercel]
entrypoint = "librarysite.wsgi:application"

[tool.vercel.scripts]
build = "python -c 'import django; django.setup();' && python manage.py migrate"